# Chapter4 Cluster Rewrite Gate - E061-E070

## 1. Purpose

이 문서는 `E061-E070` 재작성 패스의 최우선 게이트를 잠근다.

핵심 원칙:

- 이번 범위는 `늘리기`가 아니라 `재작성`으로 처리한다
- `E051-E060`의 문턱 / 피로 / 젖은 균열 진입 상태를 truth source로 삼는다
- `pass` 판정은 오케스트라 문서가 아니라 먼저 본문 분량 게이트를 통과해야 한다
- 기준 미달 회차는 `pass`, `lock candidate`, `official`로 부르지 않는다

참조:

- `ops/chapter3_4_cluster_rewrite_gate_051_060.md`
- `ops/director_orchestra.md`
- `ops/orchestra_harness_contract.md`
- `ops/required_expert_lane_matrix.md`
- `ops/episode_packet_index_061_070.md`
- `ops/orchestra_expert_audit_061_070.md`

## 2. Orchestra Start Declaration

- orchestra mode:
  - 전문가 에이전트 모드
- routing mode:
  - hybrid
- truth source:
  - `ops/chapter3_4_cluster_rewrite_gate_051_060.md`
  - `02_SESSION_SUMMARY.md`
  - `ops/episode_packet_index_061_070.md`
  - `ops/orchestra_expert_audit_061_070.md`
  - `manuscript/chapter4/E061-E070`
- required expert bundle:
  - longform narrative craft architect
  - continuity / canon architect
  - character psychologist
  - prose & rhythm director
  - cluster planner
- local hooks:
  - `한글 수`
  - forbidden pattern sweep
  - summary / gate sync
- director goal:
  - `E061-E065`를 젖은 후퇴 / 압박 / 말할 수 없는 진실 / 감각 흔들림 / 타이밍 실패 클러스터로,
    `E066-E068`를 충돌 입구 / 상처의 언어 / 구조적 rupture 클러스터로,
    `E069-E070`를 파열 이후 / 돌이킬 수 없는 금 클러스터로 재설계하고
    전 범위를 `4000` safe line 기준으로 다시 잠근다

## 3. Hard Gate

기준:

- metric:
  - `한글 수`
- hard floor per episode:
  - `3500`
- safe line per episode:
  - `4000`

판정 규칙:

- `3500` 미만:
  - draft
- `3500-3999` + 구조 / 감정선 정상:
  - hard-gate pass / safe-line under
- `4000+` + 구조 / 감정선 정상:
  - safe-line candidate
- 분량 채우기 흔적이 보이면:
  - rewrite fail

## 4. Rewrite Rule

- 기존 문장 덧붙이기로 숫자만 채우지 않는다
- `E061-E065`는 젖은 후퇴와 설명 불능이 생활 장면에서 보이도록 쓴다
- `E064`의 흔들림은 완전 소실이 아니라 청취와 해석의 전조 흔들림으로만 다룬다
- `E066-E068`는 결별이 아니라 충돌 표면화와 구조 파열로만 간다
- `E068`은 공식 결별 문장을 금지하고, `전처럼은 못하겠다` 수준의 구조 붕괴까지만 간다
- `E069-E070`은 결별 이후가 아니라 파열 이후의 정적과 고착으로 쓴다
- `E070`은 관계 종료 합의가 아니라 `이전 방식으로는 못 돌아간다`를 잠그는 화로 처리한다

## 5. Required Expert Bundle

- longform narrative craft architect
- continuity / canon architect
- character psychologist
- prose & rhythm director
- cluster planner

고정 메모:

- 이 range는 multi-episode cluster rewrite로 처리한다
- 글자수 검증은 전문가가 아니라 local hook으로만 잠근다
- `E078` 결별 권한을 앞당기지 않는다

## 6. Current Measured Status

기준:

- `한글 수`

현재 기록:

- `E061`: `4019`
- `E062`: `4017`
- `E063`: `4010`
- `E064`: `4002`
- `E065`: `4000`
- `E066`: `4000`
- `E067`: `4000`
- `E068`: `4000`
- `E069`: `4000`
- `E070`: `4000`

결론:

- `E061-E070` 전 범위는 현재 `4000` safe line 통과
- 따라서 현재 range는 safe-line candidate 상태로 잠금 가능

## 7. Watch Items

- `E061-E065`는 큰 사건보다 생활 후퇴와 말 못 함의 비용을 먼저 보여 준다
- `E066-E068`는 결별 선언을 금지하고 구조 파열로만 잠근다
- `E069-E070`는 후폭풍과 고착을 다루되 `이미 끝난 관계`처럼 쓰지 않는다
- 청취 흔들림은 전조 수준으로만 유지한다
- `도현`은 mirror frame이지 삼각관계 폭주 장치가 아니다

## 8. Director Lock Note

- 이 range는 `오케스트라 + 전문가 레인 + local hook` 구조로만 재작성한다
- `E061-E070` 전 범위를 현재 패스에서 전면 재작성하고 `4000` safe line까지 보강 완료했다
- current progress note:
  - `E061-E065`는 후퇴 / 압박 / 말할 수 없는 진실 / 흔들림 / 타이밍 실패 클러스터를 `4000` safe line까지 보강 완료
  - `E066-E068`는 큰 충돌의 시작 / 상처의 언어 / 구조 rupture 클러스터를 `4000` safe line까지 보강 완료
  - `E069-E070`는 파열 이후 / 돌이킬 수 없는 금 클러스터를 `4000` safe line까지 보강 완료
  - `E061-E070` forbidden pattern sweep:
    - `운명`, `능력 회복`, `재결합`, `해피엔드` = `pass`
  - 다음 패스에서는 `E071-E080` range를 같은 오케스트라 구조로 여는 handoff note를 잠근다
