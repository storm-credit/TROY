# Chapter3 Cluster Rewrite Gate - E041-E050

## 1. Purpose

이 문서는 `chapter3 / E041-E050` 재작성 패스의 최우선 게이트를 잠근다.

핵심 원칙:

- 이번 범위는 `늘리기`가 아니라 `재작성`으로 처리한다
- `E031-E040`의 공개 연애 시작 / 첫 키스 상태를 truth source로 삼는다
- `pass` 판정은 오케스트라 문서가 아니라 먼저 본문 분량 게이트를 통과해야 한다
- 기준 미달 회차는 `pass`, `lock candidate`, `official`로 부르지 않는다

참조:

- `ops/chapter2_late_cluster_rewrite_gate_031_040.md`
- `ops/director_orchestra.md`
- `ops/orchestra_harness_contract.md`
- `ops/required_expert_lane_matrix.md`
- `ops/episode_packet_index_041_050.md`

## 2. Orchestra Start Declaration

- orchestra mode:
  - 전문가 에이전트 모드
- routing mode:
  - hybrid
- truth source:
  - `ops/chapter2_late_cluster_rewrite_gate_031_040.md`
  - `02_SESSION_SUMMARY.md`
  - `ops/episode_packet_index_041_050.md`
  - `ops/E041-E050 episode harnesses`
  - `manuscript/chapter3/E041-E050`
- required expert bundle:
  - longform narrative craft architect
  - continuity / canon architect
  - character psychologist
  - prose & rhythm director
  - cluster planner
- local hooks:
  - `공백 제외 한글 수`
  - forbidden pattern sweep
  - summary / gate sync
- director goal:
  - `E041-E045`를 공개 연애 확장 / Dohyun mirror cluster로,
    `E046-E050`를 읽힘의 불편함 / diary seed cluster로 재설계하고
    회차별 `3500` hard floor와 `4000` safe line 기준으로 다시 잠근다

## 3. Hard Gate

기준:

- metric:
  - `공백 제외 한글 수`
- hard floor per episode:
  - `3500`
- safe line per episode:
  - `4000`

판정 규칙:

- `3500` 미만:
  - draft
- `3500-3999` + 구조 / 감정선 정상:
  - hard-gate pass / safe-line under
- `4000+` + 구조 / 감정선 정상:
  - safe-line candidate
- 분량 채우기 흔적이 보이면:
  - rewrite fail

## 4. Rewrite Rule

- 기존 문장 덧붙이기로 숫자만 채우지 않는다
- `E041-E050`는 두 개의 cluster로 본다
- `E041-E045`는 공개 연애의 확장, 외부 시선 압박, Dohyun false-right choice, 비교 불안의 점층으로 간다
- `E046-E050`는 행복의 리듬 아래에서 읽힘의 불편함, 빠른 해석, 친구 개입, diary seed의 점층으로 간다
- Dohyun은 rival / villain이 아니라 서준 불안을 비추는 `false-right choice` 거울로 둔다
- `E047-E050`는 능력 reveal 구간이 아니라 `너무 빨리 읽히는 불편함`이 커지는 구간으로 잠근다
- `E050`의 일기 문장은 이미지나 본문 바깥에서 직접 읽히는 정답으로 다루지 않는다

## 5. Required Expert Bundle

- longform narrative craft architect
- continuity / canon architect
- character psychologist
- prose & rhythm director
- cluster planner

고정 메모:

- 이 range는 multi-episode cluster rewrite로 처리한다
- 글자수 검증은 전문가가 아니라 local hook으로만 잠근다
- 다음 회차를 설계할 때 직전 회차 residue를 명시적으로 가져간다

## 6. Current Measured Status

기준:

- `공백 제외 한글 수`

현재 기록:

- `E041`: `3746`
- `E042`: `3508`
- `E043`: `3673`
- `E044`: `3781`
- `E045`: `3501`
- `E046`: `3500`
- `E047`: `3502`
- `E048`: `3500`
- `E049`: `3502`
- `E050`: `3506`

결론:

- 현재 `E041-E050` 전부 hard gate 통과
- safe line under:
  - `E041-E050` 전부
- 본 range는 hard-gate pass 기준으로 잠금 가능하지만 safe-line watchlist를 유지한다

## 7. Watch Items

- 공개 연애 확장을 해피엔딩처럼 쓰지 않는다
- 외부 시선을 악역 군중처럼 처리하지 않는다
- Dohyun을 위협 인물로 과장하지 않는다
- `읽힌다`는 불편함은 boundary issue이지 능력 reveal이 아니다

## 8. Director Lock Note

- 이 range는 `오케스트라 + 전문가 레인 + local hook` 구조로만 재작성한다
- `E041`부터 순차 재작성하고, 각 편마다 실제 수치를 기록한 뒤 다음 편으로 넘긴다
- `E041-E045`를 먼저 안정화한 뒤 `E046-E050`의 불편함 seed cluster로 넘어간다
- forbidden pattern sweep:
  - `운명`, `능력 회복`, `재결합`, `해피엔드` -> `pass`
- current lock:
  - `E041-E045`는 공개 연애 확장 / Dohyun mirror cluster로 잠금
  - `E046-E050`는 행복의 리듬 -> 읽힘의 불편함 -> diary seed cluster로 잠금
