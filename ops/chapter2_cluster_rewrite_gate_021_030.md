# Chapter2 Cluster Rewrite Gate - E021-E030

## 1. Purpose

이 문서는 `chapter2 / E021-E030` 재작성 패스의 최우선 게이트를 잠근다.

핵심 원칙:

- 이번 범위는 `늘리기`가 아니라 `재작성`으로 처리한다
- `chapter1` handoff check를 truth source로 삼는다
- `pass` 판정은 오케스트라 문서가 아니라 먼저 본문 분량 게이트를 통과해야 한다
- 기준 미달 회차는 하네스 `pass`, `lock candidate`, `official`로 부르지 않는다

참조:

- `ops/chapter1_handoff_check_before_chapter2.md`
- `ops/director_orchestra.md`
- `ops/orchestra_harness_contract.md`
- `ops/required_expert_lane_matrix.md`

## 2. Orchestra Start Declaration

- orchestra mode:
  - 전문가 에이전트 모드
- routing mode:
  - hybrid
- truth source:
  - `ops/chapter1_handoff_check_before_chapter2.md`
  - `02_SESSION_SUMMARY.md`
  - `ops/episode_packet_index_021_030.md`
  - `manuscript/chapter2/E021-E030`
- required expert bundle:
  - longform narrative craft architect
  - continuity / canon architect
  - character psychologist
  - prose & rhythm director
  - cluster planner
- local hooks:
  - `공백 제외 한글 수`
  - forbidden pattern sweep
  - summary / gate sync
- director goal:
  - `E021-E030`를 고백 준비 -> 질문 -> 고백 -> 대답 cluster로 재설계하고
    회차별 `3500` hard floor와 `4000` safe line 기준으로 다시 잠근다

## 3. Hard Gate

기준:

- metric:
  - `공백 제외 한글 수`
- hard floor per episode:
  - `3500`
- safe line per episode:
  - `4000`

판정 규칙:

- `3500` 미만:
  - draft
- `3500-3999` + 구조/감정선 정상:
  - hard-gate pass / safe-line under
- `4000+` + 구조/감정선 정상:
  - safe-line candidate
- 분량 채우기 흔적이 보이면:
  - rewrite fail

## 4. Rewrite Rule

- 기존 문장 덧붙이기로 숫자만 채우지 않는다
- `E021-E030`는 개별 회차가 아니라 하나의 confession-prep cluster로 본다
- 각 화는 `장소`가 아니라 `행동 기능`으로 분리한다
- `chapter1`에서 넘긴 관계 속도 상한을 지킨다
- `직접적 상호애 확정 이전` watch item을 `E021-E028`에 강하게 유지한다

## 5. Required Expert Bundle

- longform narrative craft architect
- continuity / canon architect
- character psychologist
- prose & rhythm director
- cluster planner

고정 메모:

- 이 range는 multi-episode cluster rewrite로 처리한다
- 글자수 검증은 전문가가 아니라 local hook으로만 잠근다
- 다음 회차를 설계할 때 직전 회차 residue를 명시적으로 가져간다

## 6. Current Measured Status

기준:

- `공백 제외 한글 수`

현재 기록:

- `E021`: `3508`
- `E022`: `3507`
- `E023`: `3570`
- `E024`: `3506`
- `E025`: `5013`
- `E026`: `3997`
- `E027`: `3737`
- `E028`: `3608`
- `E029`: `3541`
- `E030`: `3502`

결론:

- 현재 `E021-E030` 전부 hard gate 통과
- safe line pass:
  - `E025`
- safe line under:
  - `E021`, `E022`, `E023`, `E024`, `E026`, `E027`, `E028`, `E029`, `E030`
- `E021-E028`은 pre-confession pressure cluster로,
  `E029-E030`은 confession / response cluster로 기능 분리 유지
- 현재 상태는 range-level hard-gate pass이며 safe-line watchlist 유지

## 7. Watch Items From Chapter1 Handoff

- `같이 / 오래 남 / 조금` 계열 어휘 피로 분산
- 관계 속도는 계속 `직접적 상호애 확정 이전` 수준으로 유지
- `E029-E030`의 고백/대답도 승리형 멜로드라마가 아니라
  서로의 속도를 확인하는 전환점으로 잠근다

## 8. Director Lock Note

- `chapter2 / E021-E030`는 `chapter1` handoff check 이후에만 full production으로 연다
- 이 range는 현재부터 `오케스트라 + 전문가 레인 + local hook` 구조로만 재작성한다
- `E021`부터 순차 재작성하고, 각 편마다 실제 수치를 기록한 뒤 다음 편으로 넘긴다
- 이번 pass에서 `E025-E030`는 전문가 레인 메모를 반영해 전면 재작성함
- forbidden pattern sweep:
  - `운명`, `능력 회복`, `재결합`, `해피엔드` = `pass`
