# Chapter3-4 Cluster Rewrite Gate - E051-E060

## 1. Purpose

이 문서는 `E051-E060` 재작성 패스의 최우선 게이트를 잠근다.

핵심 원칙:

- 이번 범위는 `늘리기`가 아니라 `재작성`으로 처리한다
- `E041-E050`의 읽힘의 불편함 / diary seed 상태를 truth source로 삼는다
- `pass` 판정은 오케스트라 문서가 아니라 먼저 본문 분량 게이트를 통과해야 한다
- 기준 미달 회차는 `pass`, `lock candidate`, `official`로 부르지 않는다

참조:

- `ops/chapter3_cluster_rewrite_gate_041_050.md`
- `ops/director_orchestra.md`
- `ops/orchestra_harness_contract.md`
- `ops/required_expert_lane_matrix.md`
- `ops/episode_packet_index_051_060.md`

## 2. Orchestra Start Declaration

- orchestra mode:
  - 전문가 에이전트 모드
- routing mode:
  - hybrid
- truth source:
  - `ops/chapter3_cluster_rewrite_gate_041_050.md`
  - `02_SESSION_SUMMARY.md`
  - `ops/episode_packet_index_051_060.md`
  - `ops/E051-E060 episode harnesses`
  - `manuscript/chapter3/E051-E060`
- required expert bundle:
  - longform narrative craft architect
  - continuity / canon architect
  - character psychologist
  - prose & rhythm director
  - cluster planner
- local hooks:
  - `공백 제외 한글 수`
  - forbidden pattern sweep
  - summary / gate sync
- director goal:
  - `E051-E054`를 행복 최대치 / 미래 환상 / 아린 창작 정점 클러스터로,
    `E055-E060`를 작은 금 / 말하지 않은 것들 / 잘못 읽은 마음 / 문턱 클러스터로 재설계하고
    회차별 `3500` hard floor를 먼저 넘기고, 가능하면 같은 패스에서 `4000` safe line까지 올리는 기준으로 다시 잠근다

## 3. Hard Gate

기준:

- metric:
  - `공백 제외 한글 수`
- hard floor per episode:
  - `3500`
- safe line per episode:
  - `4000`

판정 규칙:

- `3500` 미만:
  - draft
- `3500-3999` + 구조 / 감정선 정상:
  - hard-gate pass / safe-line under
- `4000+` + 구조 / 감정선 정상:
  - safe-line candidate
- 분량 채우기 흔적이 보이면:
  - rewrite fail

## 4. Rewrite Rule

- 기존 문장 덧붙이기로 숫자만 채우지 않는다
- `E051-E060`는 두 개의 cluster로 본다
- `E051-E054`는 행복의 최대치, 미래 환상, 익숙한 행복, 아린 음악 세계의 정점으로 간다
- `E055-E060`는 작은 금, 말하지 않은 것들, 사랑 언어의 차이, 직접적 오독, 피로 누적, 문턱으로 간다
- `E051-E054`는 충분히 밝아야 하지만 `영원`, `결말`, `완료`의 어휘로 닫지 않는다
- `E054`는 아린의 창작 주체성이 중심이며 공연 과시나 완결 선언으로 쓰지 않는다
- `E055-E060`는 파국을 과장하지 않고 작은 금에서 단계적으로 내려간다
- `E058`의 직접 오독은 능력 공개가 아니라 관계 문제로만 다룬다
- `E060`은 결별이 아니라 Phase IV `젖은 균열` 문턱까지만 간다

## 5. Required Expert Bundle

- longform narrative craft architect
- continuity / canon architect
- character psychologist
- prose & rhythm director
- cluster planner

고정 메모:

- 이 range는 multi-episode cluster rewrite로 처리한다
- 글자수 검증은 전문가가 아니라 local hook으로만 잠근다
- 다음 회차를 설계할 때 직전 회차 residue를 명시적으로 가져간다

## 6. Current Measured Status

기준:

- `공백 제외 한글 수`

현재 기록:

- `E051`: `4347`
- `E052`: `4001`
- `E053`: `4001`
- `E054`: `4027`
- `E055`: `4000`
- `E056`: `4002`
- `E057`: `4000`
- `E058`: `4001`
- `E059`: `4030`
- `E060`: `4000`

결론:

- `E051-E060` 전 범위는 현재 `4000` safe line 통과
- 따라서 현재 range는 safe-line candidate 상태로 잠금 가능

## 7. Watch Items

- 행복 구간을 해피엔딩처럼 닫지 않는다
- 미래 환상을 실제 약속이나 상호 합의처럼 쓰지 않는다
- 아린 창작 정점을 관계 종속 장면으로 만들지 않는다
- 균열은 작은 금부터 시작한다
- `E058-E060`은 조기 결별, 능력 공개, 조기 폭발을 금지한다

## 8. Director Lock Note

- 이 range는 `오케스트라 + 전문가 레인 + local hook` 구조로만 재작성한다
- `E051`부터 순차 재작성하고, 각 편마다 실제 수치를 기록한 뒤 다음 편으로 넘긴다
- `E051-E054`를 먼저 안정화한 뒤 `E055-E060`의 금과 문턱 cluster로 넘어간다
- current progress note:
  - `E051-E054`는 행복 정점 cluster 재작성본을 `4000` safe line까지 보강 완료
  - `E055-E060`는 금 / 말하지 않은 것들 / 사랑 언어 차이 / 직접 오독 / 피로 / 문턱 cluster를 `4000` safe line까지 보강 완료
  - `E051-E060` forbidden pattern sweep:
    - `운명`, `능력 회복`, `재결합`, `해피엔드` = `pass`
  - 다음 패스에서는 `E061-E070` range를 여는 handoff note와 다음 클러스터 엔진을 잠근다
