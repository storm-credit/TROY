# Chapter2 Late Cluster Rewrite Gate - E031-E040

## 1. Purpose

이 문서는 `chapter2 / E031-E040` 재작성 패스의 최우선 게이트를 잠근다.

핵심 원칙:

- 이번 범위는 `늘리기`가 아니라 `재작성`으로 처리한다
- `E029-E030`의 고백 / 대답 상태를 truth source로 삼는다
- `pass` 판정은 오케스트라 문서가 아니라 먼저 본문 분량 게이트를 통과해야 한다
- 기준 미달 회차는 `pass`, `lock candidate`, `official`로 부르지 않는다

참조:

- `ops/chapter2_cluster_rewrite_gate_021_030.md`
- `ops/director_orchestra.md`
- `ops/orchestra_harness_contract.md`
- `ops/required_expert_lane_matrix.md`
- `ops/episode_packet_index_031_040.md`

## 2. Orchestra Start Declaration

- orchestra mode:
  - 전문가 에이전트 모드
- routing mode:
  - hybrid
- truth source:
  - `ops/chapter2_cluster_rewrite_gate_021_030.md`
  - `02_SESSION_SUMMARY.md`
  - `ops/episode_packet_index_031_040.md`
  - `ops/E031-E040 episode harnesses`
  - `manuscript/chapter2/E029-E040`
- required expert bundle:
  - longform narrative craft architect
  - continuity / canon architect
  - character psychologist
  - prose & rhythm director
  - cluster planner
- local hooks:
  - `공백 제외 한글 수`
  - forbidden pattern sweep
  - summary / gate sync
- director goal:
  - `E031-E035`를 고백 이후 어색한 조율 cluster로,
    `E036-E040`를 연애 시작과 여름 과열 초입 cluster로 재설계하고
    회차별 `3500` hard floor와 `4000` safe line 기준으로 다시 잠근다

## 3. Hard Gate

기준:

- metric:
  - `공백 제외 한글 수`
- hard floor per episode:
  - `3500`
- safe line per episode:
  - `4000`

판정 규칙:

- `3500` 미만:
  - draft
- `3500-3999` + 구조 / 감정선 정상:
  - hard-gate pass / safe-line under
- `4000+` + 구조 / 감정선 정상:
  - safe-line candidate
- 분량 채우기 흔적이 보이면:
  - rewrite fail

## 4. Rewrite Rule

- 기존 문장 덧붙이기로 숫자만 채우지 않는다
- `E031-E040`는 두 개의 cluster로 본다
- `E031-E035`는 어색한 기쁨, 속도차, 기대/현실, 닫힘, 재조율의 점층으로 간다
- `E036-E040`는 공식 관계 시작, 가까운 행복, 둘만의 세계, 안정의 표면, 첫 키스의 점층으로 간다
- `E034`는 완전한 무음이 아니라 닫힘 / 멀어짐으로 처리한다
- `E036-E040`는 해피엔딩처럼 닫지 않는다
- `E040`의 키스는 동의와 여운의 문턱이지 이해 완성의 증거가 아니다

## 5. Required Expert Bundle

- longform narrative craft architect
- continuity / canon architect
- character psychologist
- prose & rhythm director
- cluster planner

고정 메모:

- 이 range는 multi-episode cluster rewrite로 처리한다
- 글자수 검증은 전문가가 아니라 local hook으로만 잠근다
- 다음 회차를 설계할 때 직전 회차 residue를 명시적으로 가져간다

## 6. Current Measured Status

기준:

- `공백 제외 한글 수`

현재 기록:

- `E031`: `3500`
- `E032`: `3614`
- `E033`: `3753`
- `E034`: `3507`
- `E035`: `3914`
- `E036`: `3548`
- `E037`: `3555`
- `E038`: `3521`
- `E039`: `4209`
- `E040`: `3501`

결론:

- 현재 `E031-E040` 전부 hard gate 통과
- safe line pass:
  - `E039`
- safe line under:
  - `E031`, `E032`, `E033`, `E034`, `E035`, `E036`, `E037`, `E038`, `E040`
- `E031-E035`는 awkward calibration cluster로,
  `E036-E040`는 summer overheat opening cluster로 기능 분리 유지
- 현재 상태는 range-level hard-gate pass이며 safe-line watchlist 유지

## 7. Watch Items

- post-confession awkwardness를 바로 안정감으로 덮지 않는다
- 아린의 주체적 속도와 닫힘을 계속 보호한다
- 행복 구간에서도 서준의 청취 습관과 과신 씨앗을 배경에 남긴다
- `첫 키스 = 완성`으로 닫지 않는다

## 8. Director Lock Note

- 이 range는 `오케스트라 + 전문가 레인 + local hook` 구조로만 재작성한다
- `E031`부터 순차 재작성하고, 각 편마다 실제 수치를 기록한 뒤 다음 편으로 넘긴다
- `E031-E035`를 먼저 안정화한 뒤 `E036-E040`의 과열 초입으로 넘어간다
- `E031-E035` forbidden pattern sweep:
  - `운명`, `능력 회복`, `재결합`, `해피엔드` = `pass`
- `E036-E040` forbidden pattern sweep:
  - `운명`, `능력 회복`, `재결합`, `해피엔드` = `pass`
